# Notes

`generate_usd_stubs.py` is generated with AI because no tools were found that could give the same result.
Use this like `python generate_usd_stubs.py --output-dir ../stubs`.

Tools such as:
- pybind11
- pyright

Gonna checkout `https://github.com/LumaPictures/cg-stubs/blob/master/usd/stubgen_usd.py` in the future.
Possibility to get the docs from a DoxyGen documentation of OpenUSD and link it to the python stubs?